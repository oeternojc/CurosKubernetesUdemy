kind: Service
apiVersion: v1
metadata:
  name: redis
  namespace: vote  
  labels:
    app: redis  
spec:
  type: ClusterIP
  ports:
    - name: "redis-service"
      protocol: TCP
      port: 6379
      targetPort: 6379  
  selector:
    app: redis